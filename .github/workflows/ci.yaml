name: CI
 
on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
 
jobs:
  pipeline:
    uses: HospitalManagementSystem-HMS/Hospital-Management/.github/workflows/backend-template.yaml@dev
 
    with:
      service: "user-service"
      image: "ghcr.io/HospitalManagementSystem-HMS/user-service"
      sonar_key: "HospitalManagementSystem-HMS_user-service_7329dfb6-3ea5-49d4-a532-a62cc4a21a0f"
      trigger: ${{ github.event_name }}
 
    secrets: inherit